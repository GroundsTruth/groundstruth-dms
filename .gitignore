# deps
node_modules/
.pnp
.pnp.js

# next / vercel
.next/
out/
build/
.vercel/

# env & secrets — NEVER COMMIT
.env
.env.*
!.env.example

# supabase local
.supabase/
supabase/.branches/
supabase/.temp/

# misc
.DS_Store
*.log
npm-debug.log*
coverage/
.idea/
.vscode/*
!.vscode/extensions.json

# Claude Code: share project plugin enablement, keep local overrides out
.claude/settings.local.json

# Confidential client/source data — NEVER commit (sync via Drive, not git):
# client contracts, financials, driver PII, ~38MB of PDFs. Kept local-only.
artefacts/
client/
dev/
wireframes/
prompts-scm

# Only these docs are tracked at the repo root; any other root-level note stays local.
/*.md
!/README.md
!/AGENTS.md
!/CLAUDE.md
