# Handoff — 2026-06-30 (Aman + Claude)

Branch: **`feat/catalog-tax-invoice-spec`** (cut fresh off `dev`). PR → `dev`.

## What shipped
The client's **sample tax invoice** arrived and was turned into real progress.

- **`docs/INVOICE_SPEC.md` (NEW)** — reverse-engineered from the sample: exact layout, the
  **GST-inclusive** tax math (worked example), CGST/SGST-vs-IGST place-of-supply rule, the
  numbering note, and a researched **per-SKU GST/HSN table (§3a)**. Unblocks **MISSING_INPUTS #1**
  (invoice format) and **M21/M25**.
- **Catalog tax fields end-to-end (M10):** `hsn`/`taxSlabPct`/`cessPct`/`mrp`/`unitsPerCase` now in
  the `Sku` type, accessor (`SKU_COLUMNS`), server-action validate/`toRow`, the add/edit Sheet, plus a
  **Tax column + "Tax set" KPI** on `/catalog`. Columns already existed on `skus` → **no migration**.
- **Per-SKU GST/HSN researched & seeded (42/46)** into `seed-data.ts` (the seed-of-record),
  adversarially verified with citations. Carbonated + energy = 40%, water + juice = 5%, plain soda = 18%,
  cess 0. "Mix"/"Power UP" left null (unidentifiable). **PROVISIONAL — pending CA.**
- `scripts/seed-skus.ts` made **two-pass / non-destructive** (won't null tax columns it doesn't own).
- **Client decisions:** seller = **Falcon Enterprises** (GSTIN `06AIMPB2225L2ZE`); invoice numbering **deferred**.
- **`docs/CLIENT_QUESTIONNAIRE_2026-06-30.md` (NEW)** — focused client follow-up for the remaining gaps.

Verified: **101 tests** green, typecheck + `next build` clean (all 11 routes).

## ⚠️ For Hardik (cross-lane asks — also in WORKLOG)
1. **Tax reconcile:** my research refines your live category rates — plain **Soda 18%** (you set 40%),
   **Jeera 40%** (you left 'Other'=18%), **+ HSN per SKU** (you set rates only). See `INVOICE_SPEC.md` §3a.
   Agree, then apply live (re-seed or a migration). **Re-seed is HELD** until you're OK with it.
2. **Seller config:** set `config.seller` to **Falcon Enterprises / `06AIMPB2225L2ZE` / Gurugram (state 06)**
   to replace the "[GSTIN pending]" placeholder.
3. **Invoice/challan format:** build M21 invoice + M25 challan to `docs/INVOICE_SPEC.md` (was format-blocked).

## Next (Aman) — per `AMAN_KICKSTART.md`
This session was the catalog-tax slice. The handoff's priority order for my lane is:
1. 🔴 **Auth UI** (`/login` → `requestOtp`/`verifyOtp`, confirm role matrix in `AUTH_PLAN.md`, role-hide nav).
2. 🟠 **Owner Dashboard live tiles** (wire to Hardik's accessors).
3. 🟡 **Nav links** for `/inventory` `/orders` `/vans` (open cross-lane asks) + UI-kit remainder.
Catalog tax (this PR) was #4 and is now largely done pending the client/CA answers above.
