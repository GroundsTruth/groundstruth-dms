# Handoff — 2026-06-28 · Hardik + Claude

For Aman (+ his Claude): what's built on `feat/core-schema`, what's pending, where to
continue. Branch is off `dev` (contains your merged UI-kit + Catalog PR) — extends
your work, touches none of your folders.

## What I built (P13 + M01 — Phase-1 ER schema)
- **`docs/SCHEMA.md`** filled from template → the real Phase-1 ER (15 new tables).
- **Six timestamped migrations** in `supabase/migrations/` (tables + constraints only):
  | file | tables |
  |------|--------|
  | `20260628070450_core.sql` | `users`, `config`, `audit_log` |
  | `20260628070451_inventory.sql` | `stock_batches`, `stock_movements` |
  | `20260628070452_retailer.sql` | `retailers` |
  | `20260628070453_sales.sql` | `price_list`, `orders`, `order_lines`, `invoices`, `invoice_lines` |
  | `20260628070454_van.sql` | `van_loads`, `van_load_lines`, `reconciliations` |
  | `20260628070455_collection.sql` | `collections` |
- Every table follows your **`skus`/0001 pattern**: RLS on · read policy `authenticated`
  · **writes server-only** (`service_role`, no write policy) · explicit grants ·
  `updated_at` trigger (reuses your `set_updated_at()`) · `numeric >= 0` checks.
  Append-only tables (`audit_log`, `stock_movements`) granted **select+insert only**.
- All FK to your `skus`. `users.id = auth.users.id` — this is what your catalog
  `TODO(auth)` gate (createSku/updateSku) plugs into when M05–M09 land.
- Spec + plan: `docs/superpowers/specs/2026-06-28-core-schema-design.md`,
  `docs/superpowers/plans/2026-06-28-core-schema.md`. Also added
  `docs/MODULE_OWNERSHIP.md` (who-builds-what status table).

## 4 decisions I made — please sanity-check in the PR
1. **Role as enum** (`app_role`), no `roles` table — 3 fixed roles; M07 permission map = code/config.
2. **Returns = `qty_returned` column** on `van_load_lines`, not a separate `returns` table.
3. **Added `stock_movements`** append-only ledger (not explicit in tracker) — truth source for FIFO/recon/audit.
4. **Writes server-only** on every table (no write RLS policy) — RBAC enforced server-side (M07).

## NOT done here (deferred to their own module branches — by design)
- Services: `deductStock()` FIFO (M13), `confirmAndInvoice()` atomic RPC (M22),
  `reconcile()` (M27), AuditService that writes `audit_log` (M02).
- Auth flows M05–M09 (OTP/JWT/RBAC) — **shared, build WITH you**. This branch only
  creates the `users` table.
- **Migrations NOT yet applied to Supabase.** I have no live DB access from here. They
  need to be run in the **SQL Editor** in filename order (`070450`→`070455`) — each is
  idempotent. After applying, mark dates in `docs/MIGRATIONS.md`.

## From your end — what's pending / where to continue
- **Review this PR** (`feat/core-schema → dev`) — it's a shared seam (core schema +
  auth `users`). Approve or push back on the 4 decisions above.
- **Apply the 6 migrations** in the SQL Editor (or I can, once I have the keys live —
  I now have `.env.local` but `npm`/DDL access isn't wired in my shell yet).
- Still pending from **your lane** (per `docs/MODULE_OWNERSHIP.md`): finish UI-kit
  remainder, M10 catalog MRP/HSN/tax/cess (client-gated), M30/M31 live aggregates
  (wait on my tables landing), and **add the 3 Supabase vars to Vercel** (still open
  from your 2026-06-25 handoff).

## Housekeeping
- Added `.env.local` (3 Supabase keys + Maps/Resend/WhatsApp) locally — gitignored,
  **not committed**. Hardened `.gitignore`: `*env.local` + `/*.txt` (root notes stay local).
- Stale `feat/supabase-catalog` still on origin — safe to delete (already in `dev`).

## Next (me)
Apply migrations (need DB access) → then **M02 audit hook** + **M03 config**, then
inventory M11–M12. Auth M05–M09 = let's sync before either starts.
