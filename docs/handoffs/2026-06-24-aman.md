# Handoff — 2026-06-24 (Aman + Claude)

## What shipped
- **Scaffold:** Next.js 15 + React 19 + TypeScript + Tailwind v3 + shadcn/ui
  (new-york), `src/` layout — matched to the curated `package.json`. Production
  build green; lockfile committed.
- **Brand tokens** wired in `globals.css` + `tailwind.config.ts`: Campa Purple
  `#5D2081` (primary), Red `#E2231A` (alerts only), near-white background.
- **UI Kit v1:** responsive `AppShell` (desktop sidebar + mobile sheet drawer,
  active-nav highlighting), `StatusBadge` (colour always with a word), `KpiCard`,
  `QtyStepper` (driver — 48px targets, no keyboard), `PageHeader`; showcase at
  `/kit`; placeholder `/dashboard`; 13 shadcn primitives in `src/components/ui/`.
- **Coordination docs:** `CLAUDE.md`, `docs/COORDINATION.md`, `docs/STATUS.md`,
  `.gitattributes` (cross-platform line endings).

## Decisions (need Hardik's ack)
- **Dropped `@tremor/react`** — peer-requires React 18, we're on React 19; it
  wouldn't install. Owner Dashboard will use **shadcn charts (recharts)**. Same
  KPI-card aesthetic. `package.json` updated. _Alternative if you prefer Tremor:
  pin React 18 — tell me before the dashboard work._
- **Data access = server-side `@supabase/ssr`** (not browser-direct) + Supabase
  **Mumbai region**, to dodge the Jio/Airtel `*.supabase.co` DNS block. RLS
  unchanged (server client carries the user session). Rationale + the full
  Supabase-vs-alternatives cost analysis: see `CLAUDE.md` + me.

## For Hardik / Hardik's agent
- Your lane (transactional spine) folders are mapped in `docs/COORDINATION.md`.
- Foundation + UI Kit are on `feat/foundation`. Once it merges to `dev`, branch
  your `feat/*` off `dev` and build on the shell + tokens (don't re-scaffold).
- Open question for you: keep **React 19** (recommended) or pin React 18 to keep
  Tremor? Decide before dashboard/chart work.

## Not done yet (intentional)
- **Supabase + Vercel not set up** — building against seed data first.
  Vercel connects at the first PR (for the preview URL); Supabase (Mumbai) the
  cycle after, when we wire real persistence + auth.

## How to run
```
cd C:\Users\Aman\Desktop\groundstruth\groundstruth-dms
npm install
npm run dev      # → http://localhost:3000   ( / , /dashboard , /kit )
```

## Next
Owner Dashboard (real, shadcn charts) → finish UI Kit states → PR + Vercel preview.

## Update — SKU Catalog shipped (same session)
- `src/lib/catalog/`: 46 cleaned canonical SKUs + `resolveSku()` (feed
  `article_name` → SKU; alias map + signature + fuzzy; flags ambiguous / free /
  unknown instead of guessing). 10 vitest tests (`npm test`).
- `/catalog` screen — search + category filter, responsive (table on desktop,
  cards on phone), priced vs needs-rate status.
- Mobile UI fixes: mapped the `popover` colour (account dropdown was rendering
  transparent); `KpiCard` value moved to its own line so long currency never
  overlaps the icon.

## Update — Owner Dashboard shipped (same session)
- `/dashboard`: KPI row + **Sales-by-route bar chart** (shadcn chart on recharts —
  the Tremor replacement, recharts ^2.15.4, React-19 clean) + Top SKUs +
  reconciliation placeholder. Read-only, seed numbers in `src/lib/dashboard/data.ts`.
- **All three of Aman's modules are now built.** Next: connect Vercel + open the
  first PR for a preview, then wire Supabase (Mumbai) for real persistence + auth.
